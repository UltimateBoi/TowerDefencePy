# Backend API Configuration

# Flask settings
FLASK_ENV=development
PORT=5000

# Firebase Admin SDK credentials (use JSON string in production)
# FIREBASE_CREDENTIALS_JSON='{"type":"service_account","project_id":"...",...}'

# For local development, the server will use ../firebase-credentials.json
